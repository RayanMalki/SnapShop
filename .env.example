HOST=0.0.0.0
PORT=8000
GEMINI_API_KEY=
UCP_AGENT_PROFILE_URL=https://snapshop.dev/.well-known/ucp-agent.json
# Flip to false to hit Shopify's live global catalog MCP at
# https://catalog.shopify.com/api/ucp/mcp (no API key required for read-only search).
USE_MOCK_UCP=true
DATABASE_URL=sqlite:///./data/snapshop.db
UPLOAD_DIR=./uploads
